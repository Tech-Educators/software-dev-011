//we need some navigation here
//we need to use params to render data dynamically
//!remember the metadata!
export default function PostPage() {
  //here, I need to get data from database, filtering by id
  //here, I need to handle the submit for the comments form
  return (
    <>
      <h1>Individual Post Page</h1>
      {/* here, I will display the data about an individual post */}
      {/* here, I will display a form with inputs that are connected to the database columns by name */}
    </>
  );
}
